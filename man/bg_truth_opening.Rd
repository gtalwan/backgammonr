% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bg_truth_workflows.R
\name{bg_truth_opening}
\alias{bg_truth_opening}
\title{Build and cache proxy truths for opening-roll problems}
\usage{
bg_truth_opening(
  rolls = NULL,
  include_doubles = TRUE,
  budget = 8192L,
  simulation_policy = c("random", "heuristic", "aggressive", "defensive", "ts_local"),
  reward_model = c("scalar_payoff"),
  posterior_model = c("beta_pseudo"),
  n_cores = bg_default_workers_truth(),
  parallel = TRUE,
  truth_block_size = 128L,
  reference_mode = c("equal", "focused"),
  cache = FALSE,
  cache_dir = NULL,
  save_path = NULL,
  overwrite = FALSE,
  dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  crn = FALSE,
  seed = NULL,
  verbose = interactive()
)
}
\arguments{
\item{rolls}{Optional list of \code{bg_roll} objects. By default all 21 unordered
opening rolls are used when \code{include_doubles = TRUE}.}

\item{include_doubles}{Logical scalar; if \code{TRUE}, include the six opening
doubles along with the 15 non-double opening rolls.}

\item{budget}{Integer-like proxy-truth budget per opening roll.}

\item{simulation_policy}{Continuation policy used in the rollout model.}

\item{reward_model}{Reward definition used when building opening problems.
The default is the explicit scalar-payoff path; coherent alternatives
include \code{"categorical_outcome"} and \code{"win_loss"}.}

\item{posterior_model}{Posterior family used when building opening
problems. The default is the explicit pseudo-Beta scalar baseline; use a
compatible alternative when changing \code{reward_model}.}

\item{n_cores}{Integer-like worker count used inside each truth build.}

\item{parallel}{Logical scalar; if \code{FALSE}, force one worker.}

\item{truth_block_size}{Integer-like block size passed to \code{\link[=bg_reference]{bg_reference()}}.}

\item{reference_mode}{Reference allocation mode passed to \code{\link[=bg_reference]{bg_reference()}}.
\code{"equal"} remains the main proxy-reference mode; \code{"focused"} is an
approximation used to accelerate large proxy-reference builds.}

\item{cache}{Logical scalar; if \code{TRUE}, save or reuse per-opening truth
files.}

\item{cache_dir}{Optional cache directory.}

\item{save_path}{Optional path for the whole battery object.}

\item{overwrite}{Logical scalar controlling whether existing saves may be
replaced.}

\item{dice_mode}{Dice mode passed to \code{\link[=bg_reference]{bg_reference()}}.}

\item{crn}{Logical scalar indicating common-random-number use.}

\item{seed}{Optional integer seed used to derive per-opening seeds.}

\item{verbose}{Logical scalar; if \code{TRUE}, display a progress bar.}
}
\value{
A \code{bg_truth_battery} object.
}
\description{
Build and cache proxy truths for opening-roll problems
}
