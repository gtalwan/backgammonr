% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thompson_research.R
\name{certify_reference_truth}
\alias{certify_reference_truth}
\title{Certify a high-budget reference estimate}
\usage{
certify_reference_truth(
  reference = NULL,
  board = NULL,
  roll = NULL,
  reference_budget = 8192L,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L,
  unresolved_value = 0.5,
  prior_alpha = 1,
  prior_beta = 1,
  dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  crn = FALSE,
  seed = NULL
)
}
\arguments{
\item{reference}{Optional \code{bg_action_evaluation} object produced by
\code{\link[=approximate_action_truth]{approximate_action_truth()}}.}

\item{board}{Optional \code{bg_board} object. Used only when \code{reference} is NULL.}

\item{roll}{Optional \code{bg_roll} object. Used only when \code{reference} is NULL.}

\item{reference_budget}{Integer-like budget used when constructing \code{reference}
internally.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns per rollout playout.}

\item{unresolved_value}{Numeric scalar in \verb{[0, 1]} used when a rollout hits
the turn limit without finishing. A value of \code{0.5} treats unresolved
playouts as half a win.}

\item{prior_alpha}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{prior_beta}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{dice_mode}{Dice-randomization mode for rollout starts. \code{"iid"} uses
standard independent rollouts. \code{"stratified_first_roll"} and
\code{"stratified_first_two_rolls"} balance early dice outcomes.}

\item{crn}{Logical scalar; if \code{TRUE}, uses common random numbers across
candidate moves by matching rollout random streams at each sample index.}

\item{seed}{Optional integer-like scalar for reproducible rollout sampling.}
}
\value{
A list of class \code{bg_reference_certificate} with:
\itemize{
\item \code{certificate}: one-row data frame;
\item \code{reference}: the reference evaluation object.
}
}
\description{
Computes a simple top-two-gap certification diagnostic for a high-budget
reference estimate (proxy truth).
}
\details{
The returned certificate is not a proof of exact optimality. It quantifies
how separated the top two reference-estimated actions are, using an
approximate uncertainty calculation.

This helps prevent overconfident interpretation of proxy truth on hard states
with tiny estimated gaps.
}
