% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bg_engine_api.R
\name{bg_play_game}
\alias{bg_play_game}
\title{Play a full game with one selection rule for both players}
\usage{
bg_play_game(
  board = bg_initial_board(),
  roll_sequence = NULL,
  max_turns = 1000L,
  selection = c("first", "random", "aggressive", "defensive", "rollout", "equal_rollout",
    "greedy_rollout", "ucb_rollout", "ocba_rollout", "thompson_rollout", "ttts_rollout"),
  seed = NULL,
  rollout_budget = 16L,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L
)
}
\arguments{
\item{board}{A \code{bg_board} object giving the initial position.}

\item{roll_sequence}{Optional list of rolls or a single \code{bg_roll} object.}

\item{max_turns}{Integer-like scalar giving the maximum number of turns to
play.}

\item{selection}{Move-selection rule used by both players.}

\item{seed}{Optional integer-like scalar. When \code{roll_sequence} is \code{NULL},
this controls reproducible dice rolling. When \code{selection} uses randomness,
it also controls reproducible move choice.}

\item{rollout_budget}{Integer-like scalar giving the fixed total rollout budget
when \code{selection} is rollout-based.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns for each rollout playout.}
}
\value{
A \code{bg_game_result} object with nested turn history and a compact
\code{history} data frame.
}
\description{
Alternates turns, rolls dice or consumes a scripted roll sequence, generates
legal moves, selects a move, and stops when the game ends or a stopping
condition is reached.
}
\details{
Supported selection methods are the same as in \code{\link[=bg_play_turn]{bg_play_turn()}}.
}
