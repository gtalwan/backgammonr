% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_studies.R
\name{compare_methods_on_position}
\alias{compare_methods_on_position}
\title{Compare allocation methods on one fixed decision problem}
\usage{
compare_methods_on_position(
  board,
  roll,
  methods = c("thompson", "ttts", "equal", "ucb", "ocba", "greedy"),
  total_budget = 512L,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L,
  unresolved_value = 0.5,
  initial_allocations = 1L,
  ucb_exploration = 1,
  prior_alpha = 1,
  prior_beta = 1,
  dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  crn = FALSE,
  fast_diagnostics = TRUE,
  seed = NULL
)
}
\arguments{
\item{board}{A \code{bg_board} object.}

\item{roll}{A \code{bg_roll} object.}

\item{methods}{Character vector of allocation methods.}

\item{total_budget}{Integer-like scalar total rollout budget per method.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns per rollout playout.}

\item{unresolved_value}{Numeric scalar in \verb{[0, 1]} used when a rollout hits
the turn limit without finishing. A value of \code{0.5} treats unresolved
playouts as half a win.}

\item{initial_allocations}{Integer-like scalar giving the number of initial
round-robin allocations used by adaptive methods. It is ignored by the
equal-allocation baseline but retained for interface consistency.}

\item{ucb_exploration}{Exploration constant for UCB. Ignored here.}

\item{prior_alpha}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{prior_beta}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{dice_mode}{Dice-randomization mode for rollout starts. \code{"iid"} uses
standard independent rollouts. \code{"stratified_first_roll"} and
\code{"stratified_first_two_rolls"} balance early dice outcomes.}

\item{crn}{Logical scalar; if \code{TRUE}, uses common random numbers across
candidate moves by matching rollout random streams at each sample index.}

\item{seed}{Optional integer-like scalar for reproducible rollout sampling.}
}
\value{
A list of class \code{bg_method_comparison} with components:
\describe{
\item{\code{summary}}{Method-level table with recommended move, estimate, and runtime.}
\item{\code{evaluations}}{Named list of full \code{bg_action_evaluation} objects.}
\item{\code{legal_moves}}{Legal candidate move sequences used by all methods.}
\item{\code{settings}}{Study settings.}
}

Interpretation notes:
\itemize{
\item Use \code{recommended_estimate} and \code{recommended_posterior_sd} together to
avoid over-reading noisy point estimates.
\item Use \code{recommended_prob_best} / \code{recommended_expected_regret} when available
(\code{fast_diagnostics = FALSE}).
\item Runtime should be interpreted jointly with regret/PCS metrics.
}
}
\description{
Runs several rollout-allocation methods on the same board + roll with the
same total budget, then reports which move each method recommends, the
corresponding estimated value, and runtime.
}
\details{
This is the most direct "same budget, different allocation logic" comparison
for the package's core statistical question.

Statistical question addressed:
\itemize{
\item At the same budget on the same decision instance, which allocation rule
gives the strongest recommendation quality/runtime tradeoff?
\item Do adaptive methods spend budget differently than equal allocation?
}
}
\examples{
board <- bg_initial_board()
roll <- bg_roll(1L, 6L)
study <- compare_methods_on_position(
  board = board,
  roll = roll,
  methods = c("equal", "ucb", "thompson"),
  total_budget = 120L,
  rollout_policy = "random",
  max_rollout_turns = 200L,
  fast_diagnostics = TRUE,
  seed = 1L
)
study$summary
}
