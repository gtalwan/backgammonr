% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/move.R
\name{bg_move_sequence}
\alias{bg_move_sequence}
\title{Construct a full-turn move sequence}
\usage{
bg_move_sequence(player, steps = list(), roll = NULL)
}
\arguments{
\item{player}{Integer-like scalar indicating the acting player. Must be \code{1}
or \code{-1}.}

\item{steps}{A list of \code{bg_move_step} objects or step-like lists.}

\item{roll}{Optional \code{bg_roll} object or roll-like list.}
}
\value{
An object of class \code{bg_move_sequence} with fields \code{player}, \code{roll},
\code{steps}, \code{dice_used}, and \code{n_steps}.
}
\description{
Creates an ordered sequence of atomic move steps for one player's turn. This
is a structural representation only; it does not yet assert that the sequence
is legal on a particular board.
}
\details{
A move sequence must contain at least one step. Use an empty legal-move list,
not an empty move sequence, to represent a forced pass.

If a \code{roll} is supplied, the sequence is checked for internal consistency with
that roll. In particular, a die value cannot be used more times than it
appears in the roll's explicit \code{expanded} representation, which handles
doubles directly.
}
\examples{
roll <- bg_roll(3, 3)
seqn <- bg_move_sequence(
  player = 1,
  roll = roll,
  steps = list(
    bg_move_step(24, 21, 3),
    bg_move_step(24, 21, 3)
  )
)
seqn
unclass(seqn)
}
