% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bg_engine_api.R
\name{bg_legal_moves}
\alias{bg_legal_moves}
\title{Generate all legal full-turn move sequences}
\usage{
bg_legal_moves(board, roll, player = NULL)
}
\arguments{
\item{board}{A \code{bg_board} object.}

\item{roll}{A \code{bg_roll} object or roll-like list.}

\item{player}{Optional integer-like scalar giving the acting player. If
\code{NULL}, the value is taken from \code{board$turn}.}
}
\value{
A list of \code{bg_move_sequence} objects.
}
\description{
Enumerates every legal full-turn move sequence for a given board, acting
player, and dice roll.
}
\details{
The generator currently handles:
\itemize{
\item normal movement;
\item blocked points;
\item hitting blots;
\item entering from the bar;
\item the requirement to enter from the bar before any other move;
\item doubles;
\item bearing off;
\item forced-use rules:
\itemize{
\item use as many dice as legally possible;
\item if only one die can be played from a non-double roll, use the higher die
when it is playable.
}
}

The return value is a list of \code{bg_move_sequence} objects. If no legal move is
available, an empty list is returned.
}
\examples{
points <- integer(24)
points[8] <- 1L
board <- bg_board(points = points, off = c(14, 15), turn = 1L)
roll <- bg_roll(3, 2)
moves <- bg_legal_moves(board, roll)
length(moves)
moves[[1]]
}
