% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bg_truth.R
\name{bg_truth_stability}
\alias{bg_truth_stability}
\title{Rebuild proxy references to screen reference stability}
\usage{
bg_truth_stability(
  x,
  budgets = NULL,
  seeds = 1:10,
  n_cores = bg_default_workers_truth(),
  parallel = FALSE,
  truth_block_size = 128L,
  reference_mode = c("equal", "focused"),
  dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  crn = FALSE,
  progress = interactive(),
  stable_share = 0.8,
  save_path = NULL,
  overwrite = FALSE,
  seed = NULL
)
}
\arguments{
\item{x}{A \code{bg_problem}, \code{bg_truth_state}, \code{bg_truth_battery}, or a non-empty
list of problems/truth states.}

\item{budgets}{Integer-like budget ladder. When omitted, a ladder is derived
from the supplied truth object if possible.}

\item{seeds}{Integer-like replicate seed vector.}

\item{n_cores}{Integer-like worker count used inside each reference build.}

\item{parallel}{Logical scalar; if \code{TRUE}, parallelize over problem-seed
tasks.}

\item{truth_block_size}{Integer-like block size passed to \code{\link[=bg_reference]{bg_reference()}}.}

\item{reference_mode}{Reference allocation mode. Use \code{"equal"} for headline
stability studies.}

\item{dice_mode}{Dice mode passed to \code{\link[=bg_reference]{bg_reference()}}.}

\item{crn}{Logical scalar indicating common-random-number use.}

\item{progress}{Logical scalar; if \code{TRUE}, display task progress.}

\item{stable_share}{Modal-share threshold used for the
\code{reference_stability_label}.}

\item{save_path}{Optional \code{.rds} path for the returned object.}

\item{overwrite}{Logical scalar controlling replacement of an existing save.}

\item{seed}{Optional master seed used to derive per-problem/per-budget seeds.}
}
\value{
A \code{bg_truth_stability} object with raw \code{results}, per-budget
\code{summary}, and a compact \code{problem_summary}.
}
\description{
\code{bg_truth_stability()} repeatedly rebuilds proxy references over a budget
ladder and seed replicates. It is designed to separate two ideas that should
not be conflated:
}
\details{
\itemize{
\item whether the proxy-reference process itself stabilized; and
\item whether the local decision remains substantively ambiguous even after the
proxy reference has stabilized.
}

The output therefore reports both a \code{reference_stability_label} and a
\code{decision_screen_label}. These are screening summaries, not proof.
}
