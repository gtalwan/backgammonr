% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bg_truth.R
\name{bg_reference}
\alias{bg_reference}
\title{Build or extend a high-budget proxy reference}
\usage{
bg_reference(
  problem,
  budget = 4096L,
  workers_truth = bg_default_workers_truth(),
  truth_block_size = 128L,
  reference_mode = c("equal", "focused"),
  extend_existing_reference = NULL,
  dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  crn = FALSE,
  focus_top = 4L,
  focus_share = 0.75,
  seed = NULL
)
}
\arguments{
\item{problem}{A \code{bg_problem} object.}

\item{budget}{Integer-like total proxy-reference budget.}

\item{workers_truth}{Number of workers used for block simulation.}

\item{truth_block_size}{Integer-like chunk size for parallel rollout blocks.}

\item{reference_mode}{Either \code{"equal"} or \code{"focused"}. \code{"equal"} is the
conservative default. \code{"focused"} is an acceleration strategy for
proxy-reference construction and should be treated as approximate.}

\item{extend_existing_reference}{Optional \code{bg_reference} object to extend.}

\item{dice_mode}{Dice mode for proxy-reference rollouts.}

\item{crn}{Logical scalar; if \code{TRUE}, use common random numbers in the
reference engine.}

\item{focus_top}{Integer-like number of plausible top actions retained in
focused mode after the pilot screen.}

\item{focus_share}{Numeric fraction of total budget allocated to the focused
second stage.}

\item{seed}{Optional integer-like seed.}
}
\value{
A \code{bg_reference} object.
}
\description{
\code{bg_reference()} constructs the package's practical proxy-reference object.
It is explicitly \strong{not} exact backgammon truth. It is a high-budget,
model-relative reference estimate for the same rollout model defined by the
\code{bg_problem}.
}
\details{
The default equal-reference mode preserves the same rollout reward semantics
while using parallel block simulation for speed. Focused reference mode is
available as a clearly labeled approximation that screens broadly, then
spends most of the remaining budget on plausible top actions.
}
