% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_studies.R
\name{study_variance_controls}
\alias{study_variance_controls}
\title{Compare dice/CRN variance controls on one decision problem}
\usage{
study_variance_controls(
  board,
  roll,
  method = c("thompson", "ttts", "ocba", "ucb", "equal", "greedy"),
  total_budget = 1024L,
  dice_modes = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  crn_values = c(FALSE, TRUE),
  truth_budget = 8192L,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L,
  unresolved_value = 0.5,
  initial_allocations = 1L,
  ucb_exploration = 1,
  prior_alpha = 1,
  prior_beta = 1,
  fast_diagnostics = TRUE,
  truth_dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  truth_crn = FALSE,
  seed = NULL
)
}
\arguments{
\item{board}{A \code{bg_board} object.}

\item{roll}{A \code{bg_roll} object.}

\item{method}{Allocation method.}

\item{total_budget}{Integer-like rollout budget for each configuration.}

\item{dice_modes}{Character vector of dice modes to compare.}

\item{crn_values}{Logical vector of CRN settings to compare.}

\item{truth_budget}{Integer-like budget for reference truth.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns per rollout playout.}

\item{unresolved_value}{Numeric scalar in \verb{[0, 1]} used when a rollout hits
the turn limit without finishing. A value of \code{0.5} treats unresolved
playouts as half a win.}

\item{initial_allocations}{Integer-like scalar giving the number of initial
round-robin allocations used by adaptive methods. It is ignored by the
equal-allocation baseline but retained for interface consistency.}

\item{ucb_exploration}{Exploration constant for UCB. Ignored here.}

\item{prior_alpha}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{prior_beta}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{truth_dice_mode}{Dice mode used for truth construction.}

\item{truth_crn}{Logical scalar indicating CRN use in truth construction.}

\item{seed}{Optional integer-like scalar for reproducible rollout sampling.}
}
\value{
A list of class \code{bg_variance_control_study} with components:
\describe{
\item{\code{results}}{Configuration-level table with decision quality and runtime.}
\item{\code{truth}}{The high-budget \code{bg_action_evaluation} reference object.}
\item{\code{evaluations}}{Named list of per-configuration evaluations.}
\item{\code{settings}}{Study settings.}
}

Interpretation notes:
\itemize{
\item Lower \code{mse} or \code{simple_regret} indicates more stable finite-budget
estimation under the tested variance controls.
\item Effects can be state-dependent; repeat on multiple benchmark cases before
drawing broad conclusions.
}
}
\description{
Evaluates a fixed allocation method across a crossed grid of \code{dice_mode} and
\code{crn} settings, then compares each configuration to a high-budget reference.
}
\details{
This isolates variance-control effects while keeping the policy and budget
fixed.

Statistical question addressed:
\itemize{
\item How sensitive is recommendation quality to variance-control design choices?
\item Do stratification and common random numbers reduce regret/MSE for this
decision problem?
}
}
\examples{
board <- bg_initial_board()
roll <- bg_roll(1L, 6L)
var_study <- study_variance_controls(
  board = board,
  roll = roll,
  method = "thompson",
  total_budget = 320L,
  dice_modes = c("iid", "stratified_first_roll"),
  crn_values = c(FALSE, TRUE),
  truth_budget = 2000L,
  rollout_policy = "random",
  max_rollout_turns = 200L,
  fast_diagnostics = TRUE,
  seed = 1L
)
var_study$results
}
