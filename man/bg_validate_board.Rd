% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/board.R
\name{bg_validate_board}
\alias{bg_validate_board}
\title{Validate a backgammon board}
\usage{
bg_validate_board(board, error = TRUE)
}
\arguments{
\item{board}{A \code{bg_board} object or list-like board representation.}

\item{error}{Logical scalar. If \code{TRUE}, invalid boards raise an error. If
\code{FALSE}, a validation report is returned.}
}
\value{
If \code{error = TRUE}, returns \code{TRUE} invisibly for a valid board.
Otherwise returns a named list with elements \code{valid} and \code{messages}.
}
\description{
Validates structural consistency for a board representation. Validation is
performed in C++ so the same rules can be reused by later engine components.
}
\details{
The current checks cover:
\itemize{
\item presence of \code{points}, \code{bar}, \code{off}, and \code{turn} fields;
\item integer storage, expected lengths, and absence of \code{NA} values;
\item nonnegative \code{bar} and \code{off} counts;
\item \code{turn} being either \code{1L} or \code{-1L};
\item each player having exactly 15 total checkers across board, bar, and borne
off;
\item rejecting impossible states in which both players have already borne off
all 15 checkers.
}

Extra fields are currently preserved but ignored by the validator.
}
