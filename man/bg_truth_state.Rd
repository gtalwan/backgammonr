% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bg_truth_workflows.R
\name{bg_truth_state}
\alias{bg_truth_state}
\title{Build or extend a cached proxy-truth object for one state}
\usage{
bg_truth_state(
  state = NULL,
  roll = NULL,
  problem = NULL,
  budget = 8192L,
  simulation_policy = c("random", "heuristic", "aggressive", "defensive", "ts_local"),
  heuristic_policy = c("aggressive", "defensive"),
  max_rollout_turns = 220L,
  unresolved_value = 0.5,
  prior_alpha = 1,
  prior_beta = 1,
  reward_model = c("scalar_payoff"),
  posterior_model = c("beta_pseudo"),
  n_cores = bg_default_workers_truth(),
  parallel = TRUE,
  truth_block_size = 128L,
  reference_mode = c("equal", "focused"),
  cache = FALSE,
  cache_dir = NULL,
  save_path = NULL,
  overwrite = FALSE,
  dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  crn = FALSE,
  seed = NULL,
  problem_id = NULL
)
}
\arguments{
\item{state}{A \code{bg_board} object.}

\item{roll}{A \code{bg_roll} object.}

\item{problem}{Optional pre-built \code{bg_problem} object.}

\item{budget}{Integer-like proxy-truth budget.}

\item{simulation_policy}{Continuation policy inside rollouts when \code{problem}
is not supplied.}

\item{heuristic_policy}{Heuristic continuation policy used when
\code{simulation_policy = "heuristic"}.}

\item{max_rollout_turns}{Integer-like rollout horizon when \code{problem} is not
supplied.}

\item{unresolved_value}{Numeric unresolved payoff when \code{problem} is not
supplied.}

\item{prior_alpha}{Prior alpha used by the wrapped \code{bg_problem}.}

\item{prior_beta}{Prior beta used by the wrapped \code{bg_problem}.}

\item{reward_model}{Reward definition used when \code{problem} is built inside the
function. The default is the explicit scalar-payoff path; coherent
alternatives include \code{"categorical_outcome"} and \code{"win_loss"}.}

\item{posterior_model}{Posterior family used when \code{problem} is built inside
the function. The default is the explicit pseudo-Beta scalar baseline; use
a compatible alternative when changing \code{reward_model}.}

\item{n_cores}{Integer-like worker count used by the proxy-reference engine.}

\item{parallel}{Logical scalar; if \code{FALSE}, force \code{n_cores = 1}.}

\item{truth_block_size}{Integer-like block size passed to \code{\link[=bg_reference]{bg_reference()}}.}

\item{reference_mode}{Reference allocation mode passed to \code{\link[=bg_reference]{bg_reference()}}.
\code{"equal"} remains the main proxy-reference mode; \code{"focused"} is an
approximation used to accelerate large proxy-reference builds.}

\item{cache}{Logical scalar; if \code{TRUE}, reuse an existing saved truth object
when possible.}

\item{cache_dir}{Optional cache directory used when \code{save_path} is omitted.}

\item{save_path}{Optional explicit \code{.rds} path.}

\item{overwrite}{Logical scalar; if \code{TRUE}, ignore any existing saved object.}

\item{dice_mode}{Dice mode passed to \code{\link[=bg_reference]{bg_reference()}}.}

\item{crn}{Logical scalar; if \code{TRUE}, use common random numbers.}

\item{seed}{Optional integer seed.}

\item{problem_id}{Optional identifier used when \code{problem} is built inside the
function.}
}
\value{
A \code{bg_truth_state} object.
}
\description{
\code{bg_truth_state()} is the research-facing wrapper around \code{\link[=bg_reference]{bg_reference()}}. It
keeps the rollout-model estimand explicit, supports persistent caching, and
stores metadata needed to audit large proxy-truth objects.
}
