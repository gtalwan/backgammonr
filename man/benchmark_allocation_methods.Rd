% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allocation_methods.R
\name{benchmark_allocation_methods}
\alias{benchmark_allocation_methods}
\title{Benchmark rollout-allocation methods on benchmark cases}
\usage{
benchmark_allocation_methods(
  cases,
  methods = c("thompson", "ttts", "equal", "ucb", "ocba", "greedy"),
  total_budget = 32L,
  truth_budget = 256L,
  budgets = NULL,
  dice_modes = c("iid"),
  crn_values = c(FALSE),
  truth_dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  truth_crn = FALSE,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L,
  unresolved_value = 0.5,
  initial_allocations = 1L,
  ucb_exploration = 1,
  prior_alpha = 1,
  prior_beta = 1,
  seed = NULL
)
}
\arguments{
\item{cases}{A list of \code{bg_benchmark_case} objects, each containing a
\code{board}, \code{roll}, and optional \code{case_id}.}

\item{methods}{Character vector of allocation methods. Supported values
include \code{"equal"}, \code{"greedy"}, \code{"ucb"}, \code{"thompson"}, and \code{"ocba"}.}

\item{total_budget}{Integer-like scalar used when \code{budgets} is \code{NULL}.}

\item{truth_budget}{Integer-like scalar giving the large rollout budget used
to approximate truth for each case.}

\item{budgets}{Optional integer-like vector of budgets for crossed
experiments. If \code{NULL}, uses \code{total_budget}.}

\item{dice_modes}{Character vector of dice modes to compare.}

\item{crn_values}{Logical vector indicating whether to run with \code{crn = FALSE}
and/or \code{crn = TRUE}.}

\item{truth_dice_mode}{Dice mode used for truth construction.}

\item{truth_crn}{Logical scalar indicating whether truth construction uses
common random numbers.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns per rollout playout.}

\item{unresolved_value}{Numeric scalar in \verb{[0, 1]} used when a rollout hits
the turn limit without finishing. A value of \code{0.5} treats unresolved
playouts as half a win.}

\item{initial_allocations}{Integer-like scalar giving the number of initial
round-robin allocations used by adaptive methods. It is ignored by the
equal-allocation baseline but retained for interface consistency.}

\item{ucb_exploration}{Exploration constant for UCB. Ignored here.}

\item{prior_alpha}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{prior_beta}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{seed}{Optional integer-like scalar for reproducible rollout sampling.}
}
\value{
A \code{bg_allocation_benchmark} object with rectangular \code{results},
grouped \code{summary}, case-level \code{truth}, and \code{settings}.
}
\description{
Compares fixed-budget allocation rules on a collection of local backgammon
decision problems. The function first computes a large-budget approximate
truth for each case, then evaluates selected allocation methods under smaller
budgets and reports selection accuracy, simple regret, MSE, and runtime.
}
\details{
The design can be crossed over:
\itemize{
\item allocation method;
\item rollout budget;
\item dice variance-reduction mode;
\item common-random-number (CRN) setting.
}
}
\examples{
points <- integer(24)
points[8] <- 1L
points[17] <- -1L
case <- bg_benchmark_case(
  board = bg_position(points = points, off = c(14L, 14L), turn = 1L),
  roll = bg_roll(3, 2),
  case_id = "demo"
)
benchmark_allocation_methods(
  cases = list(case),
  methods = c("thompson", "ttts", "equal", "ocba"),
  budgets = c(8L, 16L),
  truth_budget = 64L,
  seed = 123
)
}
