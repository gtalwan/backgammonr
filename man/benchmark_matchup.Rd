% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmarking.R
\name{benchmark_matchup}
\alias{benchmark_matchup}
\title{Benchmark a matchup between two archetypes}
\usage{
benchmark_matchup(
  player1 = c("random", "aggressive", "defensive", "rollout", "equal_rollout",
    "greedy_rollout", "ucb_rollout", "ocba_rollout", "thompson_rollout", "ttts_rollout"),
  player2 = c("random", "aggressive", "defensive", "rollout", "equal_rollout",
    "greedy_rollout", "ucb_rollout", "ocba_rollout", "thompson_rollout", "ttts_rollout"),
  n_games = 100L,
  board = bg_initial_board(),
  max_turns = 1000L,
  roll_sequence = NULL,
  seed = NULL,
  rollout_budget = 16L,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L
)
}
\arguments{
\item{player1}{Archetype for player 1.}

\item{player2}{Archetype for player 2.}

\item{n_games}{Integer-like scalar giving the number of games to simulate.}

\item{board}{A \code{bg_board} object giving the initial position for each game.}

\item{max_turns}{Integer-like scalar giving the maximum number of turns per
game.}

\item{roll_sequence}{Optional list of rolls or a single \code{bg_roll} object.
When supplied, the same scripted roll sequence is used for every
replication.}

\item{seed}{Optional integer-like scalar for reproducible stochastic
simulations.}

\item{rollout_budget}{Integer-like scalar giving the fixed total rollout budget for
rollout-based players.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns for each rollout playout.}
}
\value{
An object of class \code{bg_matchup_benchmark} with components:
\itemize{
\item \code{games}: one row per simulated game;
\item \code{summary}: one-row aggregate benchmark summary;
\item \code{settings}: benchmark settings;
\item \code{initial_board}: the starting board.
}
}
\description{
Runs many games between two specified archetypes and records both per-game
outcomes and aggregate runtime-aware summary statistics.
}
\details{
The per-game \code{games} component is easy to convert to a tibble or other R data
frame abstraction, while the one-row \code{summary} component provides headline
benchmarking metrics such as win rate, average turns, and elapsed runtime.

Supported archetypes are:
\itemize{
\item \code{"random"}
\item \code{"aggressive"}
\item \code{"defensive"}
\item \code{"rollout"}
\item \code{"ocba_rollout"}
\item \code{"thompson_rollout"}
\item \code{"ttts_rollout"}
}
}
\examples{
board <- bg_initial_board()
bm <- benchmark_matchup(
  player1 = "random",
  player2 = "aggressive",
  n_games = 3L,
  board = board,
  max_turns = 20L,
  seed = 123
)
bm$summary

# Base-R plotting example.
summary_df <- bm$summary
barplot(
  height = summary_df$player1_win_rate,
  names.arg = paste(summary_df$player1_selection, "vs", summary_df$player2_selection),
  ylab = "Player 1 win rate",
  main = "Matchup benchmark"
)
}
