% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thompson_research.R
\name{compare_thompson_to_reference}
\alias{compare_thompson_to_reference}
\title{Compare a finite-budget Thompson run to a high-budget reference estimate}
\usage{
compare_thompson_to_reference(
  board,
  roll,
  method = c("thompson", "ttts"),
  total_budget = 512L,
  reference_budget = 8192L,
  reference = NULL,
  reference_certificate = NULL,
  ttts_beta = 0.5,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L,
  unresolved_value = 0.5,
  initial_allocations = 1L,
  prior_alpha = 1,
  prior_beta = 1,
  dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  crn = FALSE,
  fast_diagnostics = TRUE,
  reference_dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  reference_crn = FALSE,
  seed = NULL
)
}
\arguments{
\item{board}{A \code{bg_board} object.}

\item{roll}{A \code{bg_roll} object.}

\item{method}{Thompson-family method (\code{"thompson"} or \code{"ttts"}).}

\item{total_budget}{Integer-like finite simulation budget for Thompson.}

\item{reference_budget}{Integer-like high simulation budget for the reference
estimate.}

\item{reference}{Optional precomputed \code{bg_action_evaluation} object from
\code{\link[=approximate_action_reference]{approximate_action_reference()}} or \code{\link[=approximate_action_truth]{approximate_action_truth()}}. When
supplied, the function reuses it instead of recomputing reference
rollouts.}

\item{reference_certificate}{Optional precomputed \code{bg_reference_certificate}
object from \code{\link[=certify_reference_truth]{certify_reference_truth()}}. Supply this with \code{reference} to
avoid recomputing certification.}

\item{ttts_beta}{Numeric scalar in \verb{(0, 1]} used when \code{method = "ttts"}.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns per rollout playout.}

\item{unresolved_value}{Numeric scalar in \verb{[0, 1]} used when a rollout hits
the turn limit without finishing. A value of \code{0.5} treats unresolved
playouts as half a win.}

\item{initial_allocations}{Integer-like scalar giving the number of initial
round-robin allocations used by adaptive methods. It is ignored by the
equal-allocation baseline but retained for interface consistency.}

\item{prior_alpha}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{prior_beta}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{dice_mode}{Dice-randomization mode for rollout starts. \code{"iid"} uses
standard independent rollouts. \code{"stratified_first_roll"} and
\code{"stratified_first_two_rolls"} balance early dice outcomes.}

\item{crn}{Logical scalar; if \code{TRUE}, uses common random numbers across
candidate moves by matching rollout random streams at each sample index.}

\item{reference_dice_mode}{Dice mode used for reference estimation.}

\item{reference_crn}{Logical scalar for reference CRN use.}

\item{seed}{Optional integer-like scalar for reproducible rollout sampling.}
}
\value{
A list of class \code{bg_thompson_reference_comparison} with:
\itemize{
\item \code{summary}: one-row decision-quality summary;
\item \code{action_table}: action-wise finite/reference comparison table;
\item \code{evaluation}: finite-budget evaluation;
\item \code{reference}: high-budget reference estimate;
\item \code{reference_certificate}: output of \code{\link[=certify_reference_truth]{certify_reference_truth()}}.
}

Interpretation notes:
\itemize{
\item \code{proxy_pcs = 1} means the finite recommendation matches the
reference-best action.
\item \code{simple_regret} close to 0 is encouraging even when proxy PCS is 0 in very
near-tie settings.
\item Use \code{reference_certified}/\code{difficulty_label} to gauge how confidently the
reference separates the top actions.
}
}
\description{
Computes a finite-budget Thompson-family evaluation and compares it to a
high-budget reference estimate (proxy truth) on the same decision instance.
}
\details{
This is the primary one-instance evaluator for Thompson behavior: it answers
whether a finite-budget recommendation agrees with a high-budget proxy and
quantifies regret/error/runtime tradeoffs.
}
