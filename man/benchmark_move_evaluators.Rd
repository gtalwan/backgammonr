% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmarking.R
\name{benchmark_move_evaluators}
\alias{benchmark_move_evaluators}
\title{Benchmark move-evaluation archetypes on fixed decision cases}
\usage{
benchmark_move_evaluators(
  cases,
  methods = c("random", "aggressive", "defensive", "rollout", "equal_rollout",
    "greedy_rollout", "ucb_rollout", "ocba_rollout", "thompson_rollout", "ttts_rollout"),
  reference_method = NULL,
  rollout_budget = 16L,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L,
  reference_rollout_budget = rollout_budget,
  reference_rollout_policy = rollout_policy,
  reference_max_rollout_turns = max_rollout_turns,
  seed = NULL
)
}
\arguments{
\item{cases}{A non-empty list of benchmark cases created by
\code{\link[=bg_benchmark_case]{bg_benchmark_case()}}, or a single case.}

\item{methods}{Character vector of archetypes to benchmark.}

\item{reference_method}{Optional reference archetype used to compute
\code{best_move_match_rate}. When \code{NULL}, no reference comparison is performed.}

\item{rollout_budget}{Rollout budget used by rollout-based methods in
\code{methods}.}

\item{rollout_policy}{Baseline rollout policy used by rollout-based methods in
\code{methods}.}

\item{max_rollout_turns}{Maximum number of turns per rollout playout for
rollout-based methods in \code{methods}.}

\item{reference_rollout_budget}{Optional rollout budget for the
\code{reference_method} when it is rollout-based.}

\item{reference_rollout_policy}{Optional rollout policy for the
\code{reference_method} when it is rollout-based.}

\item{reference_max_rollout_turns}{Optional rollout horizon for the
\code{reference_method} when it is rollout-based.}

\item{seed}{Optional integer-like scalar for reproducible stochastic methods.}
}
\value{
An object of class \code{bg_move_evaluation_benchmark} with components:
\itemize{
\item \code{results}: one row per case-method pair;
\item \code{summary}: one row per method;
\item \code{settings}: benchmark settings.
}
}
\description{
Benchmarks one or more archetypes on a fixed collection of board-plus-roll
decision cases. For each case and method, the benchmark records:
}
\details{
\itemize{
\item the number of legal moves,
\item the chosen move index,
\item decision runtime,
\item and, when a \code{reference_method} is supplied, whether the chosen move matches
the reference move.
}

Legal moves are generated once per case and reused across methods, so the
runtime measurements focus on move evaluation and selection rather than move
generation.

\code{best_move_match_rate} is computed only over cases with more than one legal
move, because single-move cases are not informative for move discrimination.
}
\examples{
case1 <- bg_benchmark_case(
  board = bg_board(points = c(rep(0L, 7), 1L, rep(0L, 16)), off = c(14L, 15L), turn = 1L),
  roll = bg_roll(3, 2),
  case_id = "case_1"
)

bm <- benchmark_move_evaluators(
  cases = list(case1),
  methods = c("aggressive", "rollout"),
  reference_method = "rollout",
  rollout_budget = 4L,
  seed = 123
)

bm$summary

# Base-R plotting example.
barplot(
  height = bm$summary$mean_runtime_seconds,
  names.arg = bm$summary$method,
  ylab = "Mean runtime (seconds)",
  main = "Move-evaluation benchmark"
)
}
