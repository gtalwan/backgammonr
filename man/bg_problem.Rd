% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bg_problem.R
\name{bg_problem}
\alias{bg_problem}
\title{Build a one-state, one-roll Thompson-sampling decision problem}
\usage{
bg_problem(
  state,
  roll,
  simulation_policy = c("random", "heuristic", "aggressive", "defensive", "ts_local"),
  heuristic_policy = c("aggressive", "defensive"),
  max_rollout_turns = 220L,
  unresolved_value = 0.5,
  prior_alpha = 1,
  prior_beta = 1,
  reward_model = c("scalar_payoff"),
  posterior_model = c("beta_pseudo"),
  posterior_prior = NULL,
  legal_moves = NULL,
  cache = TRUE,
  problem_id = NULL
)
}
\arguments{
\item{state}{A \code{bg_board} object.}

\item{roll}{A \code{bg_roll} object.}

\item{simulation_policy}{Continuation policy used after the root action
inside rollouts. \code{"random"} is the benchmark default. \code{"heuristic"}
resolves to a fixed heuristic continuation policy. \code{"ts_local"} is
experimental because it changes the rollout-model estimand.}

\item{heuristic_policy}{Heuristic continuation policy used when
\code{simulation_policy = "heuristic"}.}

\item{max_rollout_turns}{Integer-like rollout truncation horizon.}

\item{unresolved_value}{Numeric payoff assigned to unresolved rollouts.}

\item{prior_alpha}{Positive pseudo-count used by Thompson-family summaries.}

\item{prior_beta}{Positive pseudo-count used by Thompson-family summaries.}

\item{reward_model}{Reward definition used by the rollout engine.}

\item{posterior_model}{Posterior family used by Thompson-style summaries.}

\item{posterior_prior}{Optional named list overriding the default prior for
the chosen \code{reward_model} / \code{posterior_model} pair.}

\item{legal_moves}{Optional precomputed legal move set.}

\item{cache}{Logical scalar; if \code{TRUE}, reuse a cached problem object when
the board, roll, and rollout-model settings match.}

\item{problem_id}{Optional string identifier used in repeated studies.}
}
\value{
A \code{bg_problem} object.
}
\description{
\code{bg_problem()} creates the canonical decision object used by the new
Thompson-first public API. It freezes:
}
\details{
\itemize{
\item one board state;
\item one realized dice roll;
\item one legal action set;
\item one rollout continuation policy;
\item one truncation and payoff mapping;
\item one explicit reward model and posterior model.
}

This makes the package's scientific object explicit: a fixed-budget
best-action-identification problem under Monte Carlo noise, not a claim about
exact backgammon truth.
}
