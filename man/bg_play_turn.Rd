% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bg_engine_api.R
\name{bg_play_turn}
\alias{bg_play_turn}
\title{Play a single turn}
\usage{
bg_play_turn(
  board,
  roll = NULL,
  selection = c("first", "random", "aggressive", "defensive", "rollout", "equal_rollout",
    "greedy_rollout", "ucb_rollout", "ocba_rollout", "thompson_rollout", "ttts_rollout"),
  seed = NULL,
  rollout_budget = 16L,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L
)
}
\arguments{
\item{board}{A \code{bg_board} object.}

\item{roll}{Optional \code{bg_roll} object or roll-like list. If \code{NULL}, dice are
rolled in C++.}

\item{selection}{Move-selection rule.}

\item{seed}{Optional integer-like scalar. When \code{roll = NULL}, this controls
reproducible dice rolling. When \code{selection} uses randomness, it also
controls reproducible move choice.}

\item{rollout_budget}{Integer-like scalar giving the fixed total rollout budget
when \code{selection} is rollout-based.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns for each rollout playout.}
}
\value{
A \code{bg_turn_result} object.
}
\description{
Rolls dice if needed, generates all legal full-turn move sequences, selects a
move, applies it, and returns a structured turn record.
}
\details{
Supported selection methods are:
\itemize{
\item \code{"first"}: choose the first legal move sequence returned by the engine;
\item \code{"random"}: choose uniformly at random from the legal move set;
\item \code{"aggressive"}: score resulting boards with the aggressive heuristic and
choose the highest-scoring move;
\item \code{"defensive"}: score resulting boards with the defensive heuristic and
choose the highest-scoring move;
\item \code{"rollout"}: estimate candidate win probabilities by uniform Monte Carlo
rollout and choose the move with the highest estimated win rate;
\item \code{"thompson_rollout"}: adaptively allocate rollout budget across legal
moves with Thompson sampling and choose the move with the highest
posterior mean.
\item \code{"ttts_rollout"}: adaptively allocate rollout budget using top-two
Thompson sampling aimed at fixed-budget best-action identification.
\item \code{"ocba_rollout"}: adaptively allocate rollout budget using an
OCBA-inspired targeting rule.
}

When \code{selection} is in the rollout family, unresolved rollout games count as
half a win in the reported diagnostics and contribute a fractional posterior
update for Thompson sampling.
}
\examples{
points <- integer(24)
points[8] <- 1L
points[17] <- -1L
board <- bg_board(points = points, off = c(14L, 14L), turn = 1L)
bg_play_turn(
  board,
  roll = bg_roll(3, 2),
  selection = "rollout",
  rollout_budget = 8L,
  seed = 123
)
}
