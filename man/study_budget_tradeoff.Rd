% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_studies.R
\name{study_budget_tradeoff}
\alias{study_budget_tradeoff}
\title{Study budget-vs-accuracy tradeoff for one allocation method}
\usage{
study_budget_tradeoff(
  board,
  roll,
  method = c("thompson", "ttts", "ocba", "ucb", "equal", "greedy"),
  budgets = c(128L, 512L, 2048L),
  truth_budget = 8192L,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L,
  unresolved_value = 0.5,
  initial_allocations = 1L,
  ucb_exploration = 1,
  prior_alpha = 1,
  prior_beta = 1,
  dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  crn = FALSE,
  fast_diagnostics = TRUE,
  truth_dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  truth_crn = FALSE,
  seed = NULL
)
}
\arguments{
\item{board}{A \code{bg_board} object.}

\item{roll}{A \code{bg_roll} object.}

\item{method}{Allocation method to evaluate across budgets.}

\item{budgets}{Integer-like budget vector.}

\item{truth_budget}{Integer-like budget used for the reference truth run.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns per rollout playout.}

\item{unresolved_value}{Numeric scalar in \verb{[0, 1]} used when a rollout hits
the turn limit without finishing. A value of \code{0.5} treats unresolved
playouts as half a win.}

\item{initial_allocations}{Integer-like scalar giving the number of initial
round-robin allocations used by adaptive methods. It is ignored by the
equal-allocation baseline but retained for interface consistency.}

\item{ucb_exploration}{Exploration constant for UCB. Ignored here.}

\item{prior_alpha}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{prior_beta}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{dice_mode}{Dice-randomization mode for rollout starts. \code{"iid"} uses
standard independent rollouts. \code{"stratified_first_roll"} and
\code{"stratified_first_two_rolls"} balance early dice outcomes.}

\item{crn}{Logical scalar; if \code{TRUE}, uses common random numbers across
candidate moves by matching rollout random streams at each sample index.}

\item{truth_dice_mode}{Dice mode used for the truth run.}

\item{truth_crn}{Logical scalar indicating CRN use in truth construction.}

\item{seed}{Optional integer-like scalar for reproducible rollout sampling.}
}
\value{
A list of class \code{bg_budget_tradeoff} with components:
\describe{
\item{\code{results}}{Budget-level table with recommendation quality and runtime.}
\item{\code{truth}}{The high-budget \code{bg_action_evaluation} reference object.}
\item{\code{evaluations}}{Named list of per-budget \code{bg_action_evaluation} objects.}
\item{\code{settings}}{Study settings.}
}

Interpretation notes:
\itemize{
\item \code{correct_selection} is a proxy PCS indicator against the reference-best
action.
\item \code{simple_regret} quantifies the value loss from the selected action.
\item \code{mse} measures whole-vector estimation quality, not only recommendation.
\item Improvements can be non-monotone at finite budgets due to simulation noise.
}
}
\description{
Evaluates the same method over a vector of budgets and compares each decision
to a large-budget proxy truth. This is the main single-position workflow for
studying speed/accuracy tradeoffs.
}
\details{
Statistical question addressed:
\itemize{
\item How quickly does decision quality improve as simulation budget increases?
\item Is there a practical budget region where gains begin to plateau?
}
}
\examples{
board <- bg_initial_board()
roll <- bg_roll(1L, 6L)
tradeoff <- study_budget_tradeoff(
  board = board,
  roll = roll,
  method = "thompson",
  budgets = c(120L, 360L, 900L),
  truth_budget = 2400L,
  rollout_policy = "random",
  max_rollout_turns = 200L,
  fast_diagnostics = TRUE,
  seed = 1L
)
tradeoff$results
}
