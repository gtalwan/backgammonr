% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allocation_methods.R
\name{evaluate_actions_equal}
\alias{evaluate_actions_equal}
\title{Evaluate legal moves with equal rollout allocation}
\usage{
evaluate_actions_equal(
  board,
  roll = NULL,
  legal_moves = NULL,
  total_budget = 16L,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L,
  unresolved_value = 0.5,
  initial_allocations = 1L,
  ucb_exploration = 1,
  prior_alpha = 1,
  prior_beta = 1,
  dice_mode = c("iid", "stratified_first_roll", "stratified_first_two_rolls"),
  crn = FALSE,
  fast_diagnostics = FALSE,
  trace = FALSE,
  trace_every = 1L,
  seed = NULL
)
}
\arguments{
\item{board}{A \code{bg_board} object.}

\item{roll}{Optional \code{bg_roll} object. Supply \code{roll} when \code{legal_moves} is
omitted.}

\item{legal_moves}{Optional list of legal \code{bg_move_sequence} objects. When
omitted, legal moves are generated from \code{board} and \code{roll}.}

\item{total_budget}{Integer-like scalar giving the total number of rollouts
to allocate across all legal moves.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns per rollout playout.}

\item{unresolved_value}{Numeric scalar in \verb{[0, 1]} used when a rollout hits
the turn limit without finishing. A value of \code{0.5} treats unresolved
playouts as half a win.}

\item{initial_allocations}{Integer-like scalar giving the number of initial
round-robin allocations used by adaptive methods. It is ignored by the
equal-allocation baseline but retained for interface consistency.}

\item{ucb_exploration}{Exploration constant for UCB. Ignored here.}

\item{prior_alpha}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{prior_beta}{Positive prior Beta shape parameter. Used to smooth the
posterior mean reported in the output.}

\item{dice_mode}{Dice-randomization mode for rollout starts. \code{"iid"} uses
standard independent rollouts. \code{"stratified_first_roll"} and
\code{"stratified_first_two_rolls"} balance early dice outcomes.}

\item{crn}{Logical scalar; if \code{TRUE}, uses common random numbers across
candidate moves by matching rollout random streams at each sample index.}

\item{trace}{Logical scalar; if \code{TRUE}, returns checkpoint-level allocation
traces in the \code{trace} component.}

\item{trace_every}{Integer-like scalar giving the checkpoint interval for
\code{trace}.}

\item{seed}{Optional integer-like scalar for reproducible rollout sampling.}
}
\value{
A \code{bg_action_evaluation} object with elements:
\describe{
\item{\code{results}}{data frame of candidate-level estimates, uncertainty
summaries, and move labels;}
\item{\code{recommended_index}}{1-based index of the recommended legal move;}
\item{\code{recommended_move}}{the recommended \code{bg_move_sequence} object;}
\item{\code{runtime_seconds}}{elapsed runtime for the evaluation call;}
\item{\code{trace}}{optional checkpoint-level allocation history when
\code{trace = TRUE};}
\item{\code{settings}}{evaluation settings used to construct the output.}
}
}
\description{
In package terms, a \emph{rollout} is a simulation that starts from one candidate
move, plays the game forward under a baseline policy, and records the final
outcome. In statistical language, each rollout is a Monte Carlo path used to
estimate a move-specific expected outcome, here a win probability with
unresolved games mapped to \code{unresolved_value}.
}
\details{
\code{evaluate_actions_equal()} spends a fixed total simulation budget across the
legal moves as evenly as possible. It is the main equal-allocation baseline
for comparing adaptive allocation rules such as UCB and Thompson sampling.

Statistical question addressed:
\itemize{
\item With a fixed finite budget, what can we learn about the ranking of legal
actions if we do not adapt allocation across actions?
\item How much uncertainty remains in each action estimate after equal spending?
}

Interpretation guide:
\itemize{
\item \code{estimate}: posterior mean action value under the rollout model.
\item \code{posterior_sd} and \code{lower_95}/\code{upper_95}: uncertainty in that estimate.
\item \code{allocation_count}: simulation effort spent on each action.
\item \code{prob_best} and \code{posterior_expected_regret}: optional decision diagnostics
(set \code{fast_diagnostics = FALSE} to compute them).
\item \code{recommended}: action selected by the method at budget end.
}

Limitations:
\itemize{
\item Reported values are model-relative (rollout policy, horizon, unresolved
handling, and dice settings).
\item Finite-budget uncertainty can remain large on hard positions.
\item Equal allocation is intentionally non-adaptive and may be sample-inefficient
when only a few actions are plausible best moves.
}
}
\examples{
points <- integer(24)
points[8] <- 1L
points[17] <- -1L
board <- bg_position(points = points, off = c(14L, 14L), turn = 1L)
evaluate_actions_equal(board, roll = bg_roll(3, 2), total_budget = 8L, seed = 123)
}
