% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_matchup}
\alias{simulate_matchup}
\title{Simulate repeated games between two archetypes}
\usage{
simulate_matchup(
  player1 = c("random", "aggressive", "defensive", "rollout", "equal_rollout",
    "greedy_rollout", "ucb_rollout", "ocba_rollout", "thompson_rollout", "ttts_rollout"),
  player2 = c("random", "aggressive", "defensive", "rollout", "equal_rollout",
    "greedy_rollout", "ucb_rollout", "ocba_rollout", "thompson_rollout", "ttts_rollout"),
  n_games = 100L,
  board = bg_initial_board(),
  max_turns = 1000L,
  roll_sequence = NULL,
  seed = NULL,
  include_summary = TRUE,
  rollout_budget = 16L,
  rollout_policy = c("random", "aggressive", "defensive"),
  max_rollout_turns = 1000L
)
}
\arguments{
\item{player1}{Archetype for player 1.}

\item{player2}{Archetype for player 2.}

\item{n_games}{Integer-like scalar giving the number of games to simulate.}

\item{board}{A \code{bg_board} object giving the initial position for each game.}

\item{max_turns}{Integer-like scalar giving the maximum number of turns per
game.}

\item{roll_sequence}{Optional list of rolls or a single \code{bg_roll} object.
When supplied, the same scripted roll sequence is used for every
replication.}

\item{seed}{Optional integer-like scalar. When \code{roll_sequence} is \code{NULL},
this controls reproducible dice rolling. When either player uses a
stochastic policy, it also controls reproducible move selection.}

\item{include_summary}{Logical scalar. If \code{TRUE}, a one-row summary data frame
is included in the result.}

\item{rollout_budget}{Integer-like scalar giving the fixed total rollout budget
when either player is rollout-based.}

\item{rollout_policy}{Baseline policy used inside rollout playouts.}

\item{max_rollout_turns}{Integer-like scalar giving the maximum number of
turns for each rollout playout.}
}
\value{
An object of class \code{bg_matchup_simulation} with components:
\itemize{
\item \code{games}: a data frame with one row per game;
\item \code{summary}: a one-row summary data frame, or \code{NULL};
\item \code{settings}: a list of simulation settings;
\item \code{initial_board}: the starting board used for each replication.
}
}
\description{
Simulates many games between two specified archetypes and returns a compact,
R-friendly result containing one row per game plus optional summary
statistics.
}
\details{
The currently supported archetypes are:
\itemize{
\item \code{"random"}
\item \code{"aggressive"}
\item \code{"defensive"}
\item \code{"rollout"}
\item \code{"ocba_rollout"}
\item \code{"thompson_rollout"}
\item \code{"ttts_rollout"}
}

The heavy replication loop runs in C++. Only compact per-game summaries are
returned, rather than full turn-by-turn histories, so this function is more
suitable for repeated simulation than calling \code{\link[=bg_play_game]{bg_play_game()}} in a loop.

When either player is in the rollout family (\code{"rollout"} or
\code{"thompson_rollout"}/\code{"ttts_rollout"}/\code{"ocba_rollout"}), the same rollout configuration is used by every
rollout-based player in the matchup.
}
\examples{
points <- integer(24)
points[8] <- 1L
points[17] <- -1L
board <- bg_board(points = points, off = c(14L, 14L), turn = 1L)

sim <- simulate_matchup(
  player1 = "thompson_rollout",
  player2 = "random",
  n_games = 3L,
  board = board,
  max_turns = 20L,
  rollout_budget = 4L,
  seed = 123
)

sim$games
sim$summary
}
