% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bg_truth.R
\name{bg_opening_compare_study}
\alias{bg_opening_compare_study}
\title{Compare TS and TTTS over the opening-roll battery}
\usage{
bg_opening_compare_study(
  rolls = NULL,
  include_doubles = TRUE,
  methods = c("thompson", "top_two_thompson", "equal"),
  budgets = c(16L, 32L, 64L, 128L, 256L, 512L),
  seeds = 1:30,
  proxy_truths = NULL,
  truth_budget = 4096L,
  n_cores = 1L,
  parallel = FALSE,
  progress = interactive(),
  cache_dir = NULL,
  overwrite = FALSE,
  save_path = NULL,
  bootstrap_reps = 200L,
  high_conf_threshold = 0.8,
  seed = NULL,
  ...
)
}
\arguments{
\item{rolls}{Optional vector/list of opening rolls. When omitted, the full 21
unordered opening rolls are used.}

\item{include_doubles}{Logical scalar controlling whether doubles are part of
the default opening battery.}

\item{methods}{Character vector of methods. Keep this set small and coherent.}

\item{budgets}{Integer-like budget vector.}

\item{seeds}{Integer-like seed vector for repeated method runs.}

\item{proxy_truths}{Optional opening truth source. Supply either a
\code{bg_truth_battery} object or a non-empty list of \code{bg_truth_state} objects.
When omitted, proxy references are built automatically.}

\item{truth_budget}{Integer-like equal-allocation reference budget used when
\code{proxy_truths} is not supplied.}

\item{n_cores}{Integer-like worker count used by the comparison engine.}

\item{parallel}{Logical scalar; if \code{TRUE}, parallelize repeated study tasks.}

\item{progress}{Logical scalar; if \code{TRUE}, display progress.}

\item{cache_dir}{Optional opening-truth cache directory.}

\item{overwrite}{Logical scalar controlling replacement of an existing save.}

\item{save_path}{Optional \code{.rds} path for the returned study object.}

\item{bootstrap_reps}{Integer-like number of opening-level bootstrap
replicates.}

\item{high_conf_threshold}{Numeric threshold used for the high-confidence but
wrong rate.}

\item{seed}{Optional master seed used for truth construction and bootstrap
resampling.}

\item{...}{Additional arguments passed to \code{\link[=bg_compare_algorithms]{bg_compare_algorithms()}}.}
}
\value{
A \code{bg_opening_compare_study} object.
}
\description{
\code{bg_opening_compare_study()} builds or loads opening-roll proxy references in
equal mode, runs a small coherent method set over a budget grid, summarizes
over seeds within opening roll, and only then aggregates over openings.
}
